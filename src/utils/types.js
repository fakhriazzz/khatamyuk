/**
 * Type definitions for the app (JSDoc comments for reference)
 * 
 * @typedef {Object} CalculationResult
 * @property {number} pagesPerDay - Number of pages to read per day
 * @property {number} totalDays - Total days for completion
 * @property {number} totalPages - Total pages in the Quran
 * 
 * @typedef {'home' | 'calculation' | 'result'} Step
 */

// No actual exports needed for JavaScript, but keeping file for documentation

